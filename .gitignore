# Claude Code / gstack local state (sessions, tokens, logs, QA artifacts)
.claude/
.gstack/
.docs/

# Contact form — local secrets & overrides (never commit)
supabase/.env
supabase/.env.local
assets/js/config.local.js
scripts
supabase
docs
.DS_Store
